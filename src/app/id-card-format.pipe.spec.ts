import { IdCardFormatPipe } from './id-card-format.pipe';

describe('IdCardFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IdCardFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
